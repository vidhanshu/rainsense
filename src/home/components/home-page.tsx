import React from 'react';
import HeroSection from './hero-section';

const HomePage = () => {
    return (
        <main>
            <HeroSection />
        </main>
    );
};

export default HomePage;
